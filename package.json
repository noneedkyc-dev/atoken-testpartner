{
  "name": "atoken-network-explorer",
  "version": "1.0.0",
  "private": true,
  "description": "Launch-ready static site for AToken network explorer",
  "scripts": {
    "dev": "python -m http.server 8000",
    "preview": "python -m http.server 8000"
  }
}